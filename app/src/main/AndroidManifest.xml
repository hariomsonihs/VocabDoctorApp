<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VocabDoctor"
        tools:targetApi="31">

        <!-- Splash Screen Activity (First Activity) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.VocabDoctor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (Now Second Activity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Other Activities -->
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".GrammarActivity" />
        <activity android:name=".IdiomsActivity" />
        <activity android:name=".PhrasalVerbsActivity" />
        <activity android:name=".ProverbsActivity" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".VerbsActivity" />
        <activity android:name=".DailySentenceActivity" />
        <activity android:name=".VocabularyActivity"/>
        <activity android:name=".WebViewActivity"/>

    </application>
</manifest>